# Supabase (required for Contact form + Admin messages)
# Set these in your host (Vercel, Netlify, etc.) for production – see DEPLOYMENT.md
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Admin login: comma-separated emails. Required in production for /admin access.
VITE_ADMIN_EMAILS=admin@example.com
